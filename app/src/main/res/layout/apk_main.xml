<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
android:id="@+id/apkFileLayout">
<TextView 
    android:id="@+id/aok_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="外置ＳＤ卡安装应用"
    android:textSize="40dip"
    />
   <!--  <ListView
         android:id="@+id/lvNews"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        
        android:layout_marginLeft="150dip"
        /> -->
    <GridView
        android:id="@+id/apk_grid"
        android:layout_width="850dip"
        android:layout_height="580dip"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dip"
        android:gravity="center"
        android:listSelector="@drawable/list_item"
        android:layout_below="@+id/aok_text"
        android:numColumns="5"
         />

</RelativeLayout>
